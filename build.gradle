plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
    id 'checkstyle'
    id 'org.openjfx.javafxplugin' version '0.1.0'
}

repositories {
    mavenCentral()
}

dependencies {
    def platforms = ['win', 'linux', 'mac']
    platforms.each { platform ->
        implementation "org.openjfx:javafx-controls:21.0.1:${platform}"
        implementation "org.openjfx:javafx-fxml:21.0.1:${platform}"
        implementation "org.openjfx:javafx-graphics:21.0.1:${platform}"
        implementation "org.openjfx:javafx-base:21.0.1:${platform}"
    }

    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.10.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.10.0'
}

checkstyle {
    toolVersion = '10.12.0'   // Match the plugin version
    configFile = file("config/checkstyle/checkstyle.xml")
    configProperties = ["suppressionsFile": file("config/checkstyle/suppressions.xml")]
    maxWarnings = 0   // Enforce zero warnings for full marks
}

test {
    useJUnitPlatform()

    testLogging {
        events "passed", "skipped", "failed"

        showExceptions true
        exceptionFormat "full"
        showCauses true
        showStackTraces true
        showStandardStreams = false
    }
}

javafx {
    version = '21.0.1'
    modules = ['javafx.controls', 'javafx.fxml', 'javafx.graphics']
}

application {
    mainClass.set("pranavbot.Launcher")
}

shadowJar {
    archiveBaseName.set('pranavbot')
    archiveClassifier.set('all')
    archiveVersion.set('')

    // FIX: Point to the correct configuration path
    configurations = [project.configurations.runtimeClasspath]

    manifest {
        attributes 'Main-Class': 'pranavbot.Launcher'
    }

    mergeServiceFiles()
}

run{
    standardInput = System.in
}

tasks.named('shadowJar') {
    archiveBaseName.set('pranavbot')
    archiveClassifier.set('all')
    archiveVersion.set('')   // optional – removes version from filename
    manifest {
        attributes 'Main-Class': 'pranavbot.Launcher'
    }
}

java {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}
